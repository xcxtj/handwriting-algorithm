//层序
const queue=[root]
while(queue.length){
    const level=queue.length
    for(let i=0;i<level;++i){
        const node=queue.shift()
        // ...........
    }
    if(queue.legnth){
        // 需要层数
    }
}